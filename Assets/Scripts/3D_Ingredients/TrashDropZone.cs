using UnityEngine;

[RequireComponent(typeof(Collider))]
public class TrashDropZone : MonoBehaviour
{
    // This script intentionally left empty! 
    // It acts purely as a tag for the Draggable3DItem raycast to detect.
    // The shrinking and deletion animation is already correctly implemented in IngredientVisuals.cs.
}