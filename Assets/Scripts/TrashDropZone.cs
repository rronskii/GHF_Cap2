using UnityEngine;

[RequireComponent(typeof(Collider))]
public class TrashDropZone : MonoBehaviour
{
    // This script intentionally left blank. 
    // It acts as an identifier for raycasts to find!
}